# Proxima Architecture

> **Moved.** The maintained architecture reference now lives at
> [reference/architecture.md](reference/architecture.md) — it covers the component
> map, the runtime/repo split, the runner abstraction, the concurrency/reliability
> model, and step-by-step **flows** (chat turn, goal loop, chat→wiki/workflow,
> workflow→job, schedules, run & preview).
>
> Start from the [documentation hub](README.md) for the full map of docs.
